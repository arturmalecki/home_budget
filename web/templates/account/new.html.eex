<h2>New account</h2>

<%= render "form.html", changeset: @changeset,
                        action: project_account_path(@conn, :create, @project) %>

<%= link "Back", to: project_account_path(@conn, :index, @project) %>
