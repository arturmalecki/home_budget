<h2>Edit account</h2>

<%= render "form.html", changeset: @changeset,
                        action: project_account_path(@conn, :update, @project, @account) %>

<%= link "Back", to: project_account_path(@conn, :index, @project) %>
