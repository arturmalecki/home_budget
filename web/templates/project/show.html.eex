<h2>Show project</h2>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @project.name %>
  </li>

  <li>
    <strong>User:</strong>
    <%= @project.user_id %>
  </li>

</ul>

<%= link "Edit", to: project_path(@conn, :edit, @project) %> | 
<%= link "Back", to: dashboard_path(@conn, :show) %> | 
<%= link "Accounts", to: project_account_path(@conn, :index, @project) %>
